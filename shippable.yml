language: java
services:
    - mongodb
    - redis
    - rabbitmq
before_script:
    - mkdir -p ~/clemble/config
    - cp ./config/*.properties ~/clemble/config/
after_script:
    config/move_tests.sh


